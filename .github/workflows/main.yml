name: deploy-book

# Only build when files inside the book content change (adjust paths to match your repo)
on:
  push:
    branches:
      - main
    paths:
      - "_config.yml"
      - "toc.yml"
      - "**/*.ipynb"
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy to'
        required: false
        default: 'production'
        type: choice
        options:
          - production
          - staging
      deploy_message:
        description: 'Deployment message'
        required: false
        default: 'Manual deployment'

concurrency:
  group: deploy-book-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy-book:
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write

    env:
      # R library directory that we will cache
      R_LIBS_USER: ${{ runner.temp }}/Rlibs

    steps:
      - uses: actions/checkout@v4

      # Restore cached wheelhouse for faster pip install (fallback to pip cache)
      - name: Cache Python wheelhouse
        uses: actions/cache@v4
        with:
          path: .wheels
          key: wheelhouse-${{ runner.os }}-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            wheelhouse-${{ runner.os }}-

      # Cache pip cache as additional fallback (setup-python also supports cache)
      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-cache-${{ runner.os }}-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            pip-cache-${{ runner.os }}-

      # Cache executed notebook cache; key includes notebook and book sources so we only re-execute when sources changed
      - name: Cache jupyter-book executed notebooks
        uses: actions/cache@v4
        with:
          path: _build/.jupyter_cache
          key: jupyter-book-cache-${{ runner.os }}-${{ hashFiles('**/*.ipynb', 'content/**', 'docs/**', '_config.yml', 'toc.yml') }}
          restore-keys: |
            jupyter-book-cache-${{ runner.os }}-

      # Cache R packages installed into R_LIBS_USER
      - name: Cache R packages
        uses: actions/cache@v4
        with:
          path: ${{ env.R_LIBS_USER }}
          key: r-packages-${{ runner.os }}-${{ hashFiles('**/*.R', 'DESCRIPTION', 'requirements.R') }}
          restore-keys: |
            r-packages-${{ runner.os }}-

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip' # useful but we also add explicit caches above

      # Build a local wheelhouse to avoid compilation repeated across runs (fast when cached)
      - name: Build wheelhouse (if needed)
        run: |
          python -m pip install --upgrade pip wheel setuptools
          mkdir -p .wheels
          pip wheel --wheel-dir .wheels -r requirements.txt || true
      - name: Install dependencies from wheelhouse
        run: |
          if [ -d ".wheels" ] && [ "$(ls -A .wheels)" ]; then
            pip install --no-index --find-links .wheels -r requirements.txt
          else
            pip install -r requirements.txt --prefer-binary
          fi

      # Set up R and install packages into the cached R_LIBS_USER
      - name: Set up R
        uses: r-lib/actions/setup-r@v2

      - name: Install R packages (into R_LIBS_USER)
        shell: bash
        run: |
          mkdir -p "${R_LIBS_USER}"
          R -e "install.packages(c('IRkernel','tidyverse'), lib='${R_LIBS_USER}', repos='https://cloud.r-project.org')"
          R -e "IRkernel::installspec()"

      # cache for executed notebooks already restored above, ensure jupyter-book config uses that cache (see _config.yml)
      - name: Build the book
        run: |
          # Use keep-going so build continues even if one notebook fails
          jupyter-book build . --keep-going

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "_build/html"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
